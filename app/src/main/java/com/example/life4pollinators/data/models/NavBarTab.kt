package com.example.life4pollinators.data.models

/**
 * Enum che rappresenta le tab del bottom navigation bar
 * (usato per mostrare graficamente in quale schermata si trova l'utente)
 */
enum class NavBarTab {
    Home,
    Sightings,
    AddSighting,
    Profile,
    None
}